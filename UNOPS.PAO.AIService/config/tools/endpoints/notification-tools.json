{
  "entity": "Notification",
  "description": "Manages user notifications, including retrieving, marking as read, and updating their content or status.",
  "synonyms": [
    "Alert",
    "Message",
    "Update",
    "Reminder",
    "Announcement"
  ],
  "mandatoryFields": [
    "notificationId",
    "Status"
  ],
  "endpoints": [
    {
      "name": "GetNotifications",
      "url": "api/notifications",
      "method": "GET",
      "description": "Retrieves a list of notifications for the current user. Can filter to show only unread notifications.",
      "parameters": {
        "unreadOnly": {
          "type": "boolean",
          "required": false,
          "description": "Filter to retrieve only unread notifications."
        }
      },
      "example_uses": [
        "Get all my notifications",
        "Show me only unread alerts",
        "List my recent messages",
        "Retrieve notifications, including read ones",
        "Fetch notifications for my account"
      ],
      "when_to_use": "Use this to fetch a list of notifications for display to the user, optionally filtering for unread ones."
    },
    {
      "name": "MarkAsRead",
      "url": "api/notifications/{notificationId}/read",
      "method": "PUT",
      "description": "Marks a specific notification as read for the current user to update notification status.",
      "parameters": {
        "notificationId": {
          "type": "integer",
          "required": true,
          "description": "Notification ID to mark as read"
        }
      },
      "example_uses": [
        "Mark notification 123 as read",
        "Read notification about system update",
        "Clear unread status for alert 456",
        "Mark message as seen",
        "Update notification read status"
      ],
      "when_to_use": "Use this when the user opens, reads, or acknowledges a notification to update its status."
    },
    {
      "name": "UpdateNotification",
      "url": "api/notifications/{notificationId}/update",
      "method": "PUT",
      "description": "Updates a notification's content and status for administrative or system management purposes.",
      "parameters": {
        "notificationId": {
          "type": "integer",
          "required": true,
          "description": "Notification ID to update"
        },
        "request": {
          "type": "object",
          "required": false,
          "description": "Update request containing new message and status. Requires 'Status' field.",
          "properties": {
            "Message": {
              "type": "string",
              "required": false,
              "description": "New message content for the notification (optional)."
            },
            "Status": {
              "type": "string",
              "required": true,
              "description": "New status for the notification (e.g., 'Read', 'Archived', 'Resolved')."
            }
          }
        }
      },
      "example_uses": [
        "Update notification 123's message content",
        "Change notification status to resolved",
        "Modify system alert text",
        "Update notification priority level",
        "Change notification message and status"
      ],
      "when_to_use": "Use this when administrators need to update notification content or status for system management."
    }
  ]
}