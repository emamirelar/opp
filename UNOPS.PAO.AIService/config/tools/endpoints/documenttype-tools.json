{
  "entity": "DocumentType",
  "description": "Manages document types for various entities.",
  "synonyms": [
    "Document Categories",
    "Record Types",
    "File Classifications",
    "Document Subtypes"
  ],
  "mandatoryFields": [
    "entityName"
  ],
  "baseUrl": "/api/document-type",
  "endpoints": [
    {
      "name": "GetAll",
      "url": "/api/document-type/{entityName}?pageIndex={pageIndex}&pageSize={pageSize}&orderBy={orderBy}&ascending={ascending}",
      "method": "GET",
      "intent": [
        "list",
        "get",
        "browse",
        "show"
      ],
      "description": "Retrieves all document types for a specific entity, with optional pagination and sorting.",
      "searchCapability": {
        "type": "none",
        "searchFields": [],
        "supportsPagination": true,
        "supportsSort": true
      },
      "parameters": {},
      "exampleBody": {},
      "exampleUrl": "/api/document-type/Project?pageIndex=1&pageSize=20&orderBy=Name&ascending=true",
      "requiredFields": [],
      "example_uses": [
        "Get all document types for projects.",
        "List document types for contracts, sorted by name."
      ],
      "when_to_use": "When you need to retrieve a list of document types associated with a specific entity, with optional pagination and sorting.",
      "scoring": {
        "searchBonus": 20,
        "intentMatch": 90,
        "complexity": "simple"
      }
    }
  ],
  "commonPatterns": {
    "pagination": {
      "pageIndex": "1-based page number (default: 1)",
      "pageSize": "items per page (default: 20)",
      "orderBy": "field name for sorting",
      "ascending": "sort direction boolean"
    },
    "search": {
      "simple": "use SearchPartners with searchText parameter",
      "advanced": "use AdvancedSearchPartners with searchCriteria JSON array"
    }
  }
}