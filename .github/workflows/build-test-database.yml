# =============================================================================
# BUILD TEST DATABASE IMAGE - DISABLED (pending dedicated test database)
# =============================================================================
# This workflow previously built a pre-migrated PostgreSQL Docker image by:
#   1. Spinning up a pgvector/pgvector:pg16 container
#   2. Running Identity + Application EF Core migrations
#   3. Seeding test data
#   4. Exporting the schema and publishing a Docker image to GHCR
#
# It has been disabled until a dedicated test database is provisioned.
# When ready, restore this workflow to target the real DB infrastructure.
# =============================================================================

name: Build Test Database Image (Disabled)

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is currently disabled pending a dedicated test database.'
        required: false
        default: 'disabled'
        type: string

jobs:
  placeholder:
    name: Disabled - Pending Dedicated DB
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "## Build Test Database - Disabled" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "This workflow is disabled until a dedicated test database is available." >> $GITHUB_STEP_SUMMARY
          echo "It previously built a pre-migrated PostgreSQL Docker image for E2E testing." >> $GITHUB_STEP_SUMMARY
